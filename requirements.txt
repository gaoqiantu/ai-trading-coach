# Core runtime
# NOTE: Keep this file minimal to avoid slow / flaky installs.

# OpenAI-compatible LLM client
openai>=1.59.8

# Trading data (READ-ONLY)
ccxt>=4.4.0

# Scheduling + delivery
APScheduler>=3.10.4
requests>=2.31.0

# API (OpenAI-compatible chat proxy)
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.5
